set mbox_type=Maildir

set folder=~/.Maildir/gmail
set spoolfile="+INBOX"
set mbox="INBOX"
set postponed="+[Gmail].Drafts"
mailboxes +INBOX

set smtp_url="smtp://marcos.souza.org@smtp.gmail.com:587/"
set smtp_pass = `gpg -q -d ~/.Maildir/gmail/.passwords.gpg`
set ssl_force_tls=yes

set from="marcos.souza.org@gmail.com"
set realname="Marcos Paulo de Souza"

set sort="threads"
set strict_threads="yes"

set signature="~/.signature"
set sidebar_visible = yes
set sidebar_short_path
